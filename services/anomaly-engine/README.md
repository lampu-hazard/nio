# Anomaly Engine Service

High-performance gRPC anomaly and phishing link detection service.

## Ports
- gRPC Port: `50051`

## API Interface
Defined in `proto/anomaly/v1/anomaly.proto`.

## Commands
- Run locally: `make dev`
- Run tests: `make test`
- Build release: `make build`
